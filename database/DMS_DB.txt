/**
Thông tin cấu hinh
**/

CREATE TABLE `AD_CONFIG` (
	`CODE`	TEXT,
	`VALUE`	TEXT,
	`STATUS`	INTEGER,
	`DESCRIPTION`	TEXT,
	`TYPE`	TEXT,
	`NAME`	TEXT,
	`ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(ID)
);

/***
AREA Giữ nguyên 
**/

/**
Chanel Type  - Thông
**/
CREATE TABLE `CHANNEL_TYPE` (
	`CODE`	TEXT,
	`NAME`	TEXT,
	`PARENT_ID`	INTEGER,
	`STATUS`	INTEGER,
	`IS_EDIT`	INTEGER,
	`TYPE`	INTEGER,
	`SKU`	INTEGER,
	`SALE_AMOUNT`	INTEGER,
	`ID`	INTEGER,	
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(ID)
);

/***
CUSTOMER -- Điểm lẻ
**/
CREATE TABLE `CUSTOMER` (
	`ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`CODE`	TEXT,
	`SHORT_CODE`	TEXT,
	`FIRST_CODE`	TEXT,
	`CUSTOMER_NAME`	TEXT,
	`CONTACT_NAME`	TEXT,
	`PHONE`	TEXT,
	`MOBIPHONE`	TEXT,
	`EMAIL`	TEXT,
	`CHANNEL_TYPE_ID`	INTEGER,
	`CASHIER_STAFF_ID`	INTEGER,
	`MAX_DEBIT_AMOUNT`	INTEGER,
	`MAX_DEBIT_DATE`	INTEGER,
	`AREA_ID`	TEXT,
	`HOUSENUMBER`	TEXT,
	`STREET`	TEXT,
	`ADDRESS`	TEXT,
	`REGION`	TEXT,
	`IMAGE_URL`	TEXT,
	`LAST_APPROVE_ORDER`	TEXT,
	`LAST_ORDER`	TEXT,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`INVOICE_CONPANY_NAME`	TEXT,
	`INVOICE_OUTLET_NAME`	TEXT,
	`INVOICE_TAX`	TEXT,
	`INVOICE_PAYMENT_TYPE`	TEXT,
	`INVOICE_NUMBER_ACCOUNT`	TEXT,
	`INVOICE_NAME_BANK`	TEXT,
	`DELIVERY_ADDRESS`	TEXT,
	`LAT`	REAL,
	`LNG`	REAL,
	`DISPLAY`	TEXT,
	`LOCATION`	TEXT,
	`LOYALTY`	TEXT,
	`NAME_TEXT`	TEXT,
	`APPLY_DEBIT_LIMITED`	INTEGER,
	`IDNO`	TEXT,
	`DISTANCE_ORDER`	INTEGER,
	`DELIVER_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(ID)
);

/***
Định mức bán hàng của điểm lẻ (sale_level_cat) --- Quản trị
**/
CREATE TABLE `CUSTOMER_CAT_LEVEL` (
	`CUSTOMER_CAT_LEVEL_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`SALE_LEVEL_CAT_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(CUSTOMER_CAT_LEVEL_ID)
);
/***
Cấp độ bán hàng
**/
CREATE TABLE `SALE_LEVEL_CAT` (
	`SALE_LEVEL_CAT_ID`	INTEGER,
	`CAT_ID`	INTEGER,
	`SALE_LEVEL_CODE`	TEXT,
	`SALE_LEVEL_NAME`	TEXT,
	`FROM_AMOUNT`	INTEGER,
	`TO_AMOUNT`	INTEGER,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(SALE_LEVEL_CAT_ID)
);
/***
Lịch sử kho - điểm lẻ
**/
CREATE TABLE `CUSTOMER_STOCK_HISTORY` (
	`CUSTOMER_STOCK_HISTORY_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`OBJECT_TYPE`	INTEGER,
	`OBJECT_ID`	INTEGER,
	`RESULT`	INTEGER,
	`CREATE_DATE`	TEXT,
	`IS_PASS`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(CUSTOMER_STOCK_HISTORY_ID)
);
/**
Thông tin thiết bị
**/
CREATE TABLE `DEVICE_INFO` (
	`DEVICE_INFO_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`STATUS`	INTEGER,
	`IMEI`	TEXT,
	`SERIAL`	TEXT,
	`TYPE`	INTEGER,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_USER`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(DEVICE_INFO_ID)
);

/**
Ngày lễ
**/
CREATE TABLE `EXCEPTION_DAY` (
	`EXCEPTION_DAY_ID`	INTEGER,
	`DAY_OFF`	TEXT,
	`TYPE`	INTEGER,
	`UPDATE_DATE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(EXCEPTION_DAY_ID)
);

/**
Phản hồi - đóng góp
**/
CREATE TABLE `FEEDBACK` (
	`FEEDBACK_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`TRAINING_PLAN_DETAIL_ID`	INTEGER,
	`STATUS`	INTEGER,
	`CONTENT`	TEXT,
	`TYPE`	TEXT,
	`REMIND_DATE`	TEXT,
	`DONE_DATE`	TEXT,
	`NUM_RETURN`	INTEGER,
	`CREATE_USER_ID`	INTEGER,
	`CREATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(FEEDBACK_ID)
);

/***
Phản hồi - sản phẩm
**/
CREATE TABLE `FEEDBACK_DETAIL` (
	`FEEDBACK_DETAIL_ID`	INTEGER,
	`FEEDBACK_ID`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`CREATE_USER_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(FEEDBACK_DETAIL_ID)
);

/***
Focus_>>> 
Giữ nguyên
**/

/**
Kế hoạch bán hàng
**/
CREATE TABLE `KPI_PLAN` (
	`KPI_PLAN_ID`	INTEGER,
	`ROUTING_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`MONTH`	INTEGER,
	`YEAR`	INTEGER,
	`SKU_ORDER`	INTEGER,
	`QUANTITY_ORDER`	TEXT,
	`NUM_ORDER_PLAN`	INTEGER,
	`DISPLAY`	INTEGER,
	`FOCUS1_QUANTITY`	INTEGER,
	`FOCUS2_QUANTITY`	INTEGER,
	`FOCUS3_QUANTITY`	INTEGER,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(KPI_PLAN_ID)
);

/**
Ảnh, video
**/
CREATE TABLE `MEDIA_ITEM` (
	`MEDIA_ITEM_ID`	INTEGER,
	`OBJECT_ID`	REAL,
	`URL`	TEXT,
	`THUMB_URL`	TEXT,
	`TITLE`	TEXT,
	`MEDIA_TYPE`	REAL,
	`DESCRIPTION`	TEXT,
	`FILE_SIZE`	REAL,
	`WIDTH`	REAL,
	`HEIGHT`	REAL,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_USER`	TEXT,
	`LAT`	REAL,
	`LNG`	REAL,
	`OBJECT_TYPE`	INTEGER,
	`CLIENT_THUMB_URL`	TEXT,
	`STAFF_ID`	INTEGER,
	`DISPLAY_PROGRAM_ID`	INTEGER,
	`TYPE`	INTEGER,
	`PARENT_ID`	INTEGER,
	`STATUS`	INTEGER,
	`SHOP_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(MEDIA_ITEM_ID)
);

/**
Bảng giá
**/
CREATE TABLE `PRICE` (
	`PRICE_ID`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`PRICE`	REAL,
	`PRICE_NOT_VAT`	REAL,
	`FROM_DATE`	TEXT,
	`TO_DATE`	TEXT,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`VAT`	REAL,
	`SYN_STATE`	INTEGER,
	`PACKAGE_PRICE`	INTEGER,
	`PACKAGE_PRICE_NOT_VAT`	INTEGER,
	PRIMARY KEY(PRICE_ID)
);

/**
Sản phẩm
**/
CREATE TABLE `PRODUCT` (
	`PRODUCT_ID`	INTEGER,
	`PRODUCT_CODE`	TEXT,
	`PARENT_PRODUCT_CODE`	TEXT,
	`STATUS`	INTEGER,
	`UOM1`	TEXT,
	`UOM2`	TEXT,
	`CONVFACT`	INTEGER,
	`CAT_ID`	INTEGER,
	`SUB_CAT_ID`	INTEGER,
	`BRAND_ID`	INTEGER,
	`FLAVOUR_ID`	INTEGER,
	`PACKING_ID`	INTEGER,
	`PRODUCT_TYPE`	TEXT,
	`EXPIRY_TYPE`	INTEGER,
	`EXPIRY_DATE`	INTEGER,
	`BARCODE`	TEXT,
	`PRODUCT_LEVEL_ID`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`CHECK_LOT`	INTEGER,
	`USER10`	TEXT,
	`SAFETY_STOCK`	INTEGER,
	`COMMISSION`	REAL,
	`VOLUMN`	REAL,
	`NET_WEIGHT`	REAL,
	`GROSS_WEIGHT`	REAL,
	`PRODUCT_NAME`	TEXT,
	`NAME_TEXT`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(PRODUCT_ID)
);

/**
Product Category , product group
**/
CREATE TABLE `PRODUCT_INFO` (
	`PRODUCT_INFO_CODE`	TEXT,
	`PRODUCT_INFO_NAME`	TEXT,
	`DESCRIPTION`	TEXT,
	`STATUS`	INTEGER,
	`TYPE`	INTEGER,
	`PRODUCT_INFO_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(PRODUCT_INFO_ID)
);

/***
Thông tin giới thiệu
**/
CREATE TABLE `PRODUCT_INTRODUCTION` (
	`PRODUCT_INTRODUCTION_ID`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_USER`	TEXT,
	`STATUS`	INTEGER,
	`CONTENT`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(PRODUCT_INTRODUCTION_ID)
);

/**
Promotion giữ nguyên
**/

/**
Tuyến bán hàng
**/
CREATE TABLE `ROUTING` (
	`ROUTING_ID`	INTEGER,
	`ROUTING_CODE`	TEXT,
	`ROUTING_NAME`	TEXT,
	`SHOP_ID`	INTEGER,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(ROUTING_ID)
);

/**
Chi tiết tuyến bán hàng
**/
CREATE TABLE `ROUTING_CUSTOMER` (
	`ROUTING_CUSTOMER_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`WEEK_INTERVAL`	INTEGER,
	`MONDAY`	INTEGER,
	`TUESDAY`	INTEGER,
	`WEDNESDAY`	INTEGER,
	`THURSDAY`	INTEGER,
	`FRIDAY`	INTEGER,
	`SATURDAY`	INTEGER,
	`SUNDAY`	INTEGER,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`ROUTING_ID`	INTEGER,
	`SEQ2`	INTEGER,
	`SEQ3`	INTEGER,
	`SEQ4`	INTEGER,
	`SEQ5`	INTEGER,
	`SEQ6`	INTEGER,
	`SEQ7`	INTEGER,
	`SEQ8`	INTEGER,
	`LAST_ORDER`	TEXT,
	`LAST_APPROVE_ORDER`	TEXT,
	`DAY_PLAN`	INTEGER,
	`PLAN_DATE`	TEXT,
	`DAY_PLAN_AVG`	INTEGER,
	`PLAN_AVG_DATE`	TEXT,
	`START_WEEK`	INTEGER,
	`SEQ`	INTEGER,
	`SYN_STATE`	INTEGER,
	`FROM_DATE`	TEXT,
	`TO_DATE`	TEXT,
	`QUANTITY_AVG`	real,
	PRIMARY KEY(ROUTING_CUSTOMER_ID)
);

/*** Report
Giữ nguyên
**/
/**
Số lượng ngày bán hàng trong năm
**/
CREATE TABLE `SALE_DAY` (
	`SALE_DAY_ID`	INTEGER,
	`YEAR`	TEXT,
	`T1`	INTEGER,
	`T2`	INTEGER,
	`T3`	INTEGER,
	`T4`	INTEGER,
	`T5`	INTEGER,
	`T6`	INTEGER,
	`T7`	INTEGER,
	`T8`	INTEGER,
	`T9`	INTEGER,
	`T10`	INTEGER,
	`T11`	INTEGER,
	`T12`	INTEGER,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(SALE_DAY_ID)
);

/**
Đơn đặt hàng
**/
CREATE TABLE `SALE_ORDER` (
	`SALE_ORDER_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`ORDER_DATE`	TEXT,
	`ORDER_TYPE`	TEXT,
	`ORDER_NUMBER`	TEXT,
	`APPROVED`	INTEGER,
	`TYPE`	INTEGER,
	`DELIVERY_ID`	INTEGER,
	`CAR_ID`	TEXT,
	`VAT`	REAL,
	`CASHIER_ID`	INTEGER,
	`AMOUNT`	INTEGER,
	`DISCOUNT`	INTEGER,
	`TOTAL`	INTEGER,
	`DESCRIPTION`	TEXT,
	`FROM_SALE_ORDER_ID`	INTEGER,
	`TOTAL_WEIGHT`	REAL,
	`PRIORITY`	INTEGER,
	`DELIVERY_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_USER`	TEXT,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`IS_VISIT_PLAN`	INTEGER,
	`DESTROY_CODE`	TEXT,
	`ORDER_SOURCE`	INTEGER,
	`TIME_PRINT`	TEXT,
	`DISCOUNT_AMOUNT`	INTEGER,
	`DISCOUNT_PERCENT`	INTEGER,
	`MAX_AMOUNT_FREE`	INTEGER,
	`PROGRAM_CODE`	TEXT,
	`ROUTING_ID`	INTEGER,
	`PRINT_BATCH`	INTEGER,
	`NOTE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(SALE_ORDER_ID)
);

/**
Chi tiết đơn hàng
**/
CREATE TABLE `SALE_ORDER_DETAIL` (
	`SALE_ORDER_DETAIL_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`QUANTITY`	INTEGER,
	`ORDER_DATE`	TEXT,
	`PRICE_ID`	INTEGER,
	`IS_FREE_ITEM`	INTEGER,
	`PROGRAM_TYPE`	INTEGER,
	`PROGRAM_CODE`	TEXT,
	`DISCOUNT_PERCENT`	REAL,
	`DISCOUNT_AMOUNT`	INTEGER,
	`AMOUNT`	INTEGER,
	`SALE_ORDER_ID`	INTEGER,
	`VAT`	REAL,
	`PRICE`	INTEGER,
	`TOTAL_WEIGHT`	REAL,
	`CREATE_USER`	TEXT,
	`UPDATE_USER`	TEXT,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`INVOICE_ID`	INTEGER,
	`MAX_QUANTITY_FREE`	INTEGER,
	`PRICE_NOT_VAT`	INTEGER,
	`MAX_AMOUNT_FREE`	INTEGER,
	`PROGRAME_TYPE_CODE`	TEXT,
	`SYN_STATE`	INTEGER,
	`IS_PACKAGE`	INTEGER,
	`QUANTITY_PACKAGE`	TEXT,
	`PACKAGE_PRICE`	INTEGER,
	`PACKAGE_PRICE_NOT_VAT`	INTEGER,
	PRIMARY KEY(SALE_ORDER_DETAIL_ID)
);

/***
Kế hoạch bán hàng chi tiết: Nhà phân phối - Sản phẩm
**/
CREATE TABLE `SALE_PLAN` (
	`SALE_PLAN_ID`	INTEGER,
	`PARENT_ID`	INTEGER,
	`TYPE`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`DESCRIPTION`	TEXT,
	`QUANTITY`	INTEGER,
	`AMOUNT`	INTEGER,
	`PRICE`	INTEGER,
	`MONTH`	INTEGER,
	`YEAR`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`AMOUNT_ASSIGN`	INTEGER,
	`QUANTITY_ASSIGN`	INTEGER,
	`VERSION`	INTEGER,
	`CAT_ID`	INTEGER,
	`OBJECT_TYPE`	INTEGER,
	`OBJECT_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(SALE_PLAN_ID)
);

/***
Nhà phân phối , đơn vị, khu vực
**/
CREATE TABLE `SHOP` (
	`SHOP_ID`	INTEGER,
	`SHOP_CODE`	TEXT,
	`SHOP_NAME`	TEXT,
	`PARENT_SHOP_ID`	INTEGER,
	`AREA_CODE`	TEXT,
	`PHONE`	TEXT,
	`MOBIPHONE`	TEXT,
	`SHOP_TYPE_ID`	INTEGER,
	`EMAIL`	TEXT,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`ADDRESS`	TEXT,
	`TAX_NUM`	TEXT,
	`INVOICE_NUMBER_ACCOUNT`	TEXT,
	`INVOICE_BANK_NAME`	TEXT,
	`STATUS`	INTEGER,
	`LAT`	REAL,
	`LNG`	REAL,
	`FAX`	TEXT,
	`AREA_ID`	INTEGER,
	`BILL_TO`	TEXT,
	`SHIP_TO`	TEXT,
	`CONTACT_NAME`	TEXT,
	`NAME_TEXT`	TEXT,
	`DISTANCE_ORDER`	INTEGER,
	`DISTANCE_TRAINING`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(SHOP_ID)
);

/***
Thông tin chi tiết - cấu hình nhà phân phối: Khoảng cách GPS, đặt hàng từ xa 
**/
CREATE TABLE `SHOP_PARAM` (
	`SHOP_PARAM_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`TYPE`	TEXT,
	`CODE`	TEXT,
	`NAME`	TEXT,
	`DESCRIPTION`	TEXT,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(SHOP_PARAM_ID)
);
/**
Nhân viên
**/
CREATE TABLE `STAFF` (
	`STAFF_ID`	INTEGER,
	`STAFF_CODE`	TEXT,
	`STAFF_NAME`	TEXT,
	`SHOP_ID`	INTEGER,
	`IDNO`	TEXT,
	`IDNO_PLACE`	TEXT,
	`IDNO_DATE`	TEXT,
	`EMAIL`	TEXT,
	`AREA_ID`	INTEGER,
	`STREET`	TEXT,
	`HOUSENUMBER`	TEXT,
	`ADDRESS`	TEXT,
	`PHONE`	TEXT,
	`MOBILEPHONE`	TEXT,
	`GENDER`	INTEGER,
	`STAFF_OWNER_ID`	INTEGER,
	`START_WORKING_DAY`	TEXT,
	`EDUCATION`	TEXT,
	`POSITION`	TEXT,
	`BIRTHDAY`	TEXT,
	`BIRTHDAY_PLACE`	TEXT,
	`STAFF_TYPE_ID`	INTEGER,
	`STATUS`	INTEGER,
	`LAST_APPROVED_ORDER`	TEXT,
	`LAST_ORDER`	TEXT,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`PLAN`	INTEGER,
	`UPDATE_PLAN`	TEXT,
	`SKU`	INTEGER,
	`IMEI`	TEXT,
	`RESET_THRESHOLD`	INTEGER,
	`SALE_GROUP`	TEXT,
	`NAME_TEXT`	TEXT,
	`SALE_TYPE_CODE`	TEXT,
	`DISTANCE_ORDER`	INTEGER,
	`DISTANCE_TRAINING`	INTEGER,
	`SIGN_KEY`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STAFF_ID)
);

/** 
Nhân viên - điểm lẻ
**/
CREATE TABLE `STAFF_CUSTOMER` (
	`STAFF_CUSTOMER_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`LAST_ORDER`	TEXT,
	`LAST_APPROVE_ORDER`	TEXT,
	`DAY_PLAN`	REAL,
	`DAY_PLAN_DATE`	TEXT,
	`DAY_PLAN_AVG`	REAL,
	`DAY_PLAN_AVG_DATE`	TEXT,
	`EXCEPTION_ORDER_DATE`	TEXT,
	`TOTAL_VISIT`	INTEGER,
	`REMAINING_VISIT`	INTEGER,
	`QUANTITY_AVG`	REAL,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STAFF_CUSTOMER_ID)
);

/***
????
***/
CREATE TABLE `STAFF_CUSTOMER_PLAN` (
	`STAFF_CUSTOMER_PLAN_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`CUSTOMER_ID`	INTEGER,
	`QUANTITY`	INTEGER,
	`SALE_DATE`	TEXT,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STAFF_CUSTOMER_PLAN_ID)
);
/**
Log vị trí nhân viên
**/
CREATE TABLE `STAFF_POSITION_LOG` (
	`STAFF_POSITION_LOG_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`LAT`	REAL,
	`LNG`	REAL,
	`ACCURACY`	REAL,
	`CREATE_DATE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STAFF_POSITION_LOG_ID)
);

/***
????
**/
CREATE TABLE `STAFF_SALE_CAT` (
	`STAFF_SALE_CAT_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`CAT_ID`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STAFF_SALE_CAT_ID)
);

/***
Thông tin chấm công 
**/
CREATE TABLE `STAFF_WORKDAY` (
	`STAFF_WORKDAY_ID`	INTEGER,
	`STAFF_OWNER_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`ABSENCE`	INTEGER,
	`NOTE`	TEXT,
	`ABSENCE_DAY`	TEXT,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`REASON_CODE`	TEXT,
	`NUM_HOUR_WORK`	INTEGER,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STAFF_WORKDAY_ID)
);

/***
Tồn kho nhà phân phối (OBJECT_ID: Nhà phân phối / Nhân viên)
**/
CREATE TABLE `STOCK_TOTAL` (
	`STOCK_TOTAL_ID`	INTEGER,
	`OBJECT_ID`	INTEGER,  
	`OBJECT_TYPE`	INTEGER,
	`PRODUCT_ID`	INTEGER,
	`DESCR`	TEXT,
	`QUANTITY`	INTEGER,
	`AVAILABLE_QUANTITY`	INTEGER,
	`CREATE_USER`	TEXT,
	`UPDATE_USER`	TEXT,
	`CREATE_DATE`	TEXT,
	`UPDATE_DATE`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(STOCK_TOTAL_ID)
);

/**
Kế hoạch ghé thăm
**/
CREATE TABLE `VISIT_PLAN` (
	`VISIT_PLAN_ID`	INTEGER,
	`ROUTING_ID`	INTEGER,
	`SHOP_ID`	INTEGER,
	`STAFF_ID`	INTEGER,
	`FROM_DATE`	TEXT,
	`TO_DATE`	TEXT,
	`STATUS`	INTEGER,
	`CREATE_DATE`	TEXT,
	`CREATE_USER`	TEXT,
	`UPDATE_DATE`	TEXT,
	`UPDATE_USER`	TEXT,
	`SYN_STATE`	INTEGER,
	PRIMARY KEY(VISIT_PLAN_ID)
);